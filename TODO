Fix hashchange, popstate, etc. This way anchor don't need to have an event listener for 
loading a page. E.g. explorer/search.js L77, tab creation in general.

tab.js L27 (activeTabs.forEach)

openPath also loads pages?

const $ = (id) => document.getElementById(id);
const $icon = $('explorer-search-icon');
