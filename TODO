- Don't use the search-bar component, make a function that enables functionality an existing input
- 2 grids not 1
- header pattern?
- Fix recreating recents and bookmarks every tab click
- Wrap page contents in container-fluid ms-0
- Make ESC blur() in the editor
- FileExplorer takes prefix for namespacing in storage
- Remove useless wrappers